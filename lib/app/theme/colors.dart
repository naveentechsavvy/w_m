import 'package:flutter/material.dart';

class AppColors {
  // ===========================
  // Brand Colors
  // ===========================

  static const Color primary = Color(0xFFFF5A3D);
  static const Color primaryDark = Color(0xFF2B1D1A);
  static const Color accent = Color(0xFFF4B400);
  static const Color background = Color(0xFFFFF8EC);

  // Old names (keep for compatibility)
  static const Color primaryLight = Color(0xFFFF8A70);
  static const Color primaryTint = Color(0xFFFFF1EC);

  // ===========================
  // Text
  // ===========================

  static const Color textPrimary = Color(0xFF2B1D1A);
  static const Color textSecondary = Color(0xFF6E6E6E);

  // Old names
  static const Color textDark = textPrimary;
  static const Color textGrey = textSecondary;
  static const Color textLight = Color(0xFFBDBDBD);

  // ===========================
  // Basic Colors
  // ===========================

  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // ===========================
  // UI
  // ===========================

  static const Color card = Colors.white;
  static const Color divider = Color(0xFFE9DFC9);
  static const Color shadow = Color(0x14000000);

  // ===========================
  // Status
  // ===========================

  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xFFD32F2F);
}