import 'package:flutter/material.dart';

class ExpDetailsScreen extends StatelessWidget {
  const ExpDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Experience Details"),
      ),
    );
  }
}